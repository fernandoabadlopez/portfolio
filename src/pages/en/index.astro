---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import Network from '../../components/Network.astro';

const entries = (await getCollection('case-studies', ({ data }) => data.lang === 'en')).sort(
  (a, b) => a.data.order - b.data.order,
);
---

<BaseLayout title="Fernando Abad López" lang="en">
  <div class="shell home">
    <header class="home-head">
      <h1 class="home-name">Fernando Abad López</h1>
      <p class="home-role">Real-time simulation and 3D graphics engineer.</p>
    </header>

    <!-- Home is the work index: every case study is a node in the network. -->
    <Network entries={entries} lang="en" />
  </div>
</BaseLayout>
